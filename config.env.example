# === Suite Configuration ===

# Suite log
SUITE_LOG="${SUITE_LOG:-$(pwd)/suite.log}"

# === Backup settings ===
BACKUP_SRC="${BACKUP_SRC:-$HOME}"
# Destination directory
BACKUP_DEST="${BACKUP_DEST:-/tmp/backups}"
BACKUP_EXCLUDES_FILE="${BACKUP_EXCLUDES_FILE:-$(pwd)/exclude.example.txt}"
BACKUP_RETAIN_DAYS="${BACKUP_RETAIN_DAYS:-14}"
BACKUP_DRYRUN_DEFAULT="${BACKUP_DRYRUN_DEFAULT:-false}"

# === Update settings ===
UPDATES_YES="${UPDATES_YES:-true}"

# === Log monitor thresholds ===
# % disk usage considered high
DISK_USAGE_WARN="${DISK_USAGE_WARN:-85}"
# Top N CPU processes to report
TOP_CPU_N="${TOP_CPU_N:-5}"
# Watch mode interval (seconds) for log_monitor --follow
WATCH_INTERVAL="${WATCH_INTERVAL:-10}"
